{
  "name": "auxcore",
  "version": "1.0.0",
  "description": "test",
  "author": "Brandon Lockaby",
  "license": "SEE LICENSE IN LICENSE.txt",
  "homepage": "https://github.com/brandon-lockaby",
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "copy": "cp -r static/* dist/",
    "clean-copy": "npm run clean && npm run copy",
    "debug": "npm run clean-copy && parcel index.html",
    "build": "npm run clean-copy && parcel build index.html",
    "push": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "dependencies": {
    "three": "^0.147.0"
  },
  "devDependencies": {
    "@types/three": "^0.143.1",
    "buffer": "^5.7.1",
    "parcel": "",
    "push-dir": "^0.4.1",
    "rimraf": ""
  }
}
